/**
 * Limbus Command - Firebase 配置範本
 *
 * 使用說明：
 * 1. 將此檔案重新命名為 firebase-config.js
 * 2. 將下方的配置替換成您從 Firebase Console 複製的內容
 * 3. 確保 index.html 已載入 Firebase SDK
 */

// Firebase 配置物件（請替換成您自己的配置）
const firebaseConfig = {
  apiKey: "YOUR_API_KEY_HERE",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  databaseURL: "https://YOUR_PROJECT.firebasedatabase.app",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};

// 初始化 Firebase
firebase.initializeApp(firebaseConfig);

// 獲取 Realtime Database 參考
const database = firebase.database();

console.log('✅ Firebase 已成功初始化');
console.log('📍 資料庫 URL:', firebaseConfig.databaseURL);
